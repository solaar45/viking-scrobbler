services:
  viking:
    build: .
    container_name: viking-scrobbler
    environment:
      - MIX_ENV=prod
      - PHX_SERVER=true
      - PORT=4000
      - DATABASE_PATH=/app/data/app_api.db
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
